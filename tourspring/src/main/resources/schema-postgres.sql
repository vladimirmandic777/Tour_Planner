/* DROP TABLE IF EXISTS tour;
CREATE TABLE tour(id serial PRIMARY KEY, name VARCHAR(255), description VARCHAR(255),
                  from VARCHAR(255), to VARCHAR(255), transport VARCHAR(255), distance INTEGER,
                  estimatedTime INTEGER, routeInformation VARCHAR(255) );
 */